@model IEnumerable<LexiconLMSPortal.Models.ViewModels.CourseViewModel>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div  class="col-xs-8">
        
        @foreach (var item in Model)
        {
            <a href="@Url.Action("Courses","Teacher", new { @id = item.Id})" class="TeacherLink">
                <div id="TeacherCourseList" tooltip="@Html.DisplayFor(modelItem => item.Description)">
                    <div>
                        <h1>
                            @Html.DisplayFor(modelItem => item.Name)
                        </h1>
                    </div>
                    <div class="text-center">
                        Starts:
                        @Html.DisplayFor(modelItem => item.StartDate)
                    </div>
                    <div class="text-center">
                        Ends:
                        @Html.DisplayFor(modelItem => item.EndDate)
                    </div>
                    <div class="text-right">
                        <a href="@Url.Action("Delete","Teacher")"class="TeacherLinkIcon">
                            <i class="glyphicon glyphicon-remove-circle"></i>
                        </a>
                        <a href="@Url.Action("Edit","Teacher")"class="TeacherLinkIcon">
                            <i class="glyphicon glyphicon-pencil"></i>
                        </a>
                    </div>

                </div>
            </a>
        }                 
    </div>
    <div id="TList">
        @Html.Action("_TeacherListPartial", "Teacher")
    </div>
    <div class="col-xs-8">
        @Html.Action("Create","Teacher")
    </div>
    @*@using (Html.BeginForm("Create","Teacher"))
    {
        @Html.AntiForgeryToken()
        <div class="col-xs-8 CreateFormControl">
            <h2>Create a new course</h2>
            <form method="post" action="@Url.Action("Create","TeacherController")">
                <div class="form-group col-xs-12">
                    <label for="CourseName">Course Name</label>
                    <input type="text" class="form-control" id="CourseName" placeholder="Course name" />
                </div>
                <div class="form-group col-xs-6">
                    <label for="CourseStartTime">Start date</label>
                    <input type="date" class="form-control" id="CourseStartTime" />
                </div>
                <div class="form-group col-xs-6">
                    <label for="CourseEndTime">End date</label>
                    <input type="date" class="form-control" id="CourseEndTime" />
                </div>
                <div class="form-group col-xs-10">
                    <label for="Description">Description</label>
                    <textarea class="form-control" rows="3" placeholder="Description text here..."></textarea>
                </div>
                <button type="submit" class="btn btn-default">Create</button>
            </form>
        </div>
        
    }*@
    
    
</div>


